/*
Assigment6: Will ask the user to enter an int value and after the user enters the variable, it'll
check wheter the value is a prime number or not
Author": Jose Flores
Date: 7/11/2019
*/

#include <iostream>

using namespace std;

//Prototype for the checkPrime function
bool checkPrime(int); 


int main()
{
	//The variable that will hold the user's entered value
	int input; 
	bool primeResult;
	
	//Asks the user for the value
	cout << "Enter a number to check if that number is prime: ";
	cin >> input;

	//The results of the checkPrime function is initialized onto the variable primeResult
	primeResult = checkPrime(input);
	cout << primeResult;

	if (primeResult == true)
	{
		cout << "Your value is prime \n";
	}
	
	else
	{
		cout << "Your value is not prime\n" ;
	}

	system("pause");
	return 0;
}

/*bool checkPrime(int num) 
{
	for (int i = num - 1; i > 1; i--) 
	{
		if ((num % i) == 0)
		{
			return false;
		}
	}
	return true;
}*/
bool checkPrime(int gotInput) // Need to fix -- it says that 9 is a prime number -- Should be fixed now
{
	/* or (gotInput/2) Why divide? Because, the dividers after the first half, just gives the same numbers as the first half, or they're use less*/
	for (int num = 2; num < gotInput; num++)
	{
		if (gotInput % num == 0)
		{
			cout << num << endl; //Delete, it's only a stub
			return false;
		}

	}

	return true;
}